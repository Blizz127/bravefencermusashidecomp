# Assembly layout

Retail-derived assembly is added only after the executable identity and
section boundaries are observed. Keep handwritten or generated material in
the following directories:

- `functions/` for function bodies and per-function match notes;
- `data/` for read-only and mutable data declarations;
- `labels/` for evidence-backed symbol/label manifests.

The directories are placeholders until a retail extraction is available.
